# Contrato da API — DOC Intelligence

> **Status:** não iniciado. Este arquivo é a fonte única de verdade do contrato
> (ver [ADR-0001](adr/0001-stack.md), seção 3). Os tipos em `src/types/` e os
> handlers em `src/mocks/handlers.ts` devem espelhar o que estiver aqui.

A definição dos endpoints, payloads e estados assíncronos faz parte do
desenvolvimento e ainda não foi feita.
